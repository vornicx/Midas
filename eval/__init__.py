"""Eval harness — adapter-based benchmark for agentic memory systems.

Every system-under-test (baseline, Atlas, and — later — Mem0/Zep/Letta/Supermemory)
implements the same `MemoryAdapter`, so the comparison is apples-to-apples and the
result is the project's differentiation pillar: an objective measure of context quality.
"""
